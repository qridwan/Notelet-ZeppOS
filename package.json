{
  "name": "notelet",
  "version": "1.0.0",
  "description": "Offline-first text notes, always within reach — Zepp OS watch companion.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@zeppos/device-types": "^3.0.0"
  },
  "dependencies": {
    "@zeppos/zml": "^0.0.38"
  }
}
